import pytest


@pytest.mark.integration
def test_replace_with_first_real_persistence_contract() -> None:
    """Delete this test when the project adds its first real Couchbase repository test."""
    pytest.skip("No project-specific persistence contract exists yet")
