# Couchbase schema and indexes

When the project adds persistence, keep declarative bucket/scope/collection/index definitions here and make one code/schema representation authoritative. Bootstrap scripts and integration tests should derive from or validate that source of truth.
