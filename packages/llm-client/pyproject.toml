[project]
name = "app-llm-client"
version = "0.1.0"
description = "Replaceable LLM provider boundary"
requires-python = ">=3.12,<3.14"
dependencies = ["pydantic>=2.9", "pydantic-settings>=2.6"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/app_llm"]
