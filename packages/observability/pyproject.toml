[project]
name = "app-observability"
version = "0.1.0"
description = "Structured logging and correlation utilities"
requires-python = ">=3.12,<3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/app_observability"]
