# Claude Code Instructions

The canonical repository instructions are in [`AGENTS.md`](AGENTS.md). Read and follow that file first.

Then read the project context files it names. Do not maintain a second, divergent architecture or coding policy in this file.

When finishing a substantial work session, update `docs/ai/session-handoff.md` so another assistant can continue without reconstructing the state from chat history.
